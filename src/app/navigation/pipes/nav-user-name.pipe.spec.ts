import { NavUserNamePipe } from './nav-user-name.pipe';

describe('NavUserNamePipe', () => {
  it('create an instance', () => {
    const pipe = new NavUserNamePipe();
    expect(pipe).toBeTruthy();
  });
});
